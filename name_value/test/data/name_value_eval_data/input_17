#python

for j in range (0, number_of(route/point)):
    expect route/point[j]/name != ""
    # should pass

for i in range (0, number_of(route/point)):
    expect route/point[i]/name = "whatever"
    # should fail

