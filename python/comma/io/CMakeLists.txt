IF( BUILD_CPP_PYTHON_BINDINGS )

    SET( MODULE_NAME stream )
    ADD_LIBRARY( ${MODULE_NAME} SHARED stream.cpp )
    TARGET_LINK_LIBRARIES( ${MODULE_NAME} ${Boost_LIBRARIES} ${PYTHON_LIBRARY} comma_io )
    SET_TARGET_PROPERTIES( ${MODULE_NAME} PROPERTIES PREFIX "" LIBRARY_OUTPUT_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR} )

ENDIF( BUILD_CPP_PYTHON_BINDINGS )
